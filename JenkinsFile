pipeline {
    agent any

    stages {
        stage('Checkout') {
            steps {
                git 'https://github.com/atulsharmacsk/CucumberReportPortal'
            }
        }
        stage('Build') {
            steps {
                sh 'mvn clean test'
            }
        }
    }
}
